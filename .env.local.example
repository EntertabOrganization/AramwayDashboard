# Secret used to sign/verify the admin session JWT (jose HS256).
# Copy this file to .env.local and change the value for anything beyond local dev.
JWT_SECRET=dev-only-change-me-aramway-dashboard-secret
